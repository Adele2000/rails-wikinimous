<h2><%= @article.title %></h2> <i class="fa-solid fa-trash-can"></i>
  <%= link_to article_path(article), method: :delete, data: { confirm: "Are you sure?"}  do %>
      <i class="fa-solid fa-trash-can"> </i> <br>
      <% end %>
  <p><%= @article.content %></p>
